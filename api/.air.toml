root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/uploy ."
  bin = "./tmp/uploy"
  delay = 1000
  exclude_dir = ["tmp", "node_modules"]
  exclude_regex = ["_test\\.go$"]
  include_ext = ["go", "sql"]
  kill_delay = "10s"
  send_interrupt = true
  stop_on_error = true
